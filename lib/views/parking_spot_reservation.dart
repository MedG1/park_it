import 'package:flutter/material.dart';

class SpotReservation extends StatelessWidget {
  const SpotReservation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('select a parking spot to reserve'),
      ),
      body: const Center(
        child: Text('page under construction'),
      ),
    );
  }
}
